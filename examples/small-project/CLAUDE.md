# SmallProject — Навигация для Claude

> **Философия:** `.claude/project.skills.md`

---

## Роутинг задач (СМОТРИ СЮДА ПЕРВЫМ)

| Тип задачи | Куда идти | Строки/Детали |
|------------|-----------|---------------|
| **API** | `src/api.ts` | 45 строк, можно целиком |
| **Auth** | `src/auth.ts` | 30 строк, можно целиком |
| **БД** | `prisma/schema.prisma` | — |

---

## Ключевые файлы

```
src/
├── api.ts          (45)    # API endpoints — маленький
├── auth.ts         (30)    # Auth — маленький
└── utils.ts        (20)    # Утилиты — маленький

prisma/
└── schema.prisma           # БД схема
```

Все файлы маленькие — можно читать целиком.

---

## Env переменные

```env
DATABASE_URL
API_KEY
```

---

*Создано: 2024-01-15*
