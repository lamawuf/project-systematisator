# {{PROJECT_NAME}} — Навигация для Claude

> **Философия:** `.claude/project.skills.md`
> **Глобальные правила:** `~/.claude/global.skills.md`

---

## Роутинг задач (СМОТРИ СЮДА ПЕРВЫМ)

| Тип задачи | Куда идти | Строки/Детали |
|------------|-----------|---------------|
| **Главная логика** | `{{MAIN_FILE}}` | — |
| **БД схема** | `prisma/schema.prisma` | — |
| **Auth** | `lib/auth.ts` | — |

---

## Индекс {{MAIN_FILE}} (— строк)

> Заполни после создания файла: `python scripts/update-index.py {{MAIN_FILE}}`

| Функция | Строка | Назначение |
|---------|--------|------------|
| — | — | — |

---

## API Endpoints

| Endpoint | Метод | Назначение |
|----------|-------|------------|
| — | — | — |

---

## Prisma модели

| Модель | Назначение |
|--------|------------|
| `User` | — |

---

## Ключевые файлы (с размерами)

```
src/lib/                           # Бизнес-логика
├── {{MAIN_FILE}}                  # — строк
└── ...

src/components/                    # UI
└── ...

.claude/
├── project.skills.md              # Философия проекта
└── settings.local.json            # Permissions
```

---

## Env переменные

```env
DATABASE_URL          # PostgreSQL
OPENAI_API_KEY        # AI
CLERK_*               # Auth
```

---

## Деплой

- **Платформа:** Railway / Vercel
- **URL:** —
- **После изменений схемы:** `npx prisma db push`

---

## Рецепты (частые операции)

### Деплой
```bash
npx prisma db push && railway up
```

---

## НЕ ТРАТЬ ТОКЕНЫ

1. **Большие файлы** — используй индекс, не читай целиком
2. **Схема** — только `prisma/schema.prisma`
3. **Философия** — читай `.claude/project.skills.md`

---

*Создано: {{DATE}}*
